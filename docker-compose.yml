services:
  mototwist:
    image: ghcr.io/amot-dev/mototwist:latest
    env_file:
      - .env
    ports:
      - "8000:8000"
    depends_on:
      - db

  db:
    image: postgres:17.6
    env_file:
      - .env
    volumes:
      - db:/var/lib/postgresql/data

volumes:
  db: